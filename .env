import streamlit as st
SUPABASE_URL=st.secrets["SUPABASE_URL"]
SUPABASE_ANON_KEY=st.secrets["SUPABASE_ANON_KEY"]

EMAIL_SMTP_SERVER=st.secrets["EMAIL_SMTP_SERVER"]
EMAIL_SMTP_PORT=st.secrets["EMAIL_SMTP_PORT"]
EMAIL_SMTP_USERNAME=st.secrets["EMAIL_SMTP_USERNAME"]
EMAIL_SMTP_PASSWORD=st.secrets["EMAIL_SMTP_PASSWORD"]
EMAIL_FROM=st.secrets["EMAIL_FROM"]
EMAIL_USE_TLS=st.secrets["EMAIL_USE_TLS"]
EMAIL_USE_SSL=st.secrets["EMAIL_USE_SSL"]

